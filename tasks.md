# tasks.md — Custom Content Uploader (SCORM) · Clinical Learning Hub
<!-- Ordered build work. Every task traces to a user story or acceptance criterion.
     If a task cannot be traced, it is either unnecessary or the spec is missing something.
     Reconstructed from git history (`git log --all --stat`) — see commit hashes per task.
     Review/audit passes are tracked separately under "Reviews / Audits" (R-numbered) — they
     don't build anything themselves, but every open item in "Next" traces back to one. -->

## Completed
- [x] [T1] Scaffold team working docs (`spec.md`, `plan.md`, `tasks.md`, `metrics.md`, `skills.md`, `CLAUDE.md`) — n/a (process setup) — Claire Cui (`cuic3`) — `acf2613`, `bc100d3`
- [x] [T2] Reformat the CS product brief into an agent-consumable `spec.md` structure (stable AC IDs, MUST/SHOULD framing) — n/a (spec authoring) — Claire Cui (`cuic3`) — `25421fe`
- [x] [T3] Adopt `spec.md` as the canonical spec; fill in Open Assumptions owners (§7: A1/A2/A3) — n/a (spec authoring) — Michelle Zuckerberg — `d2bbc80`, `b5a7b98`
- [x] [T4] Add real SCORM 1.2 sample package (Rustici golf course, `RuntimeBasicCalls_SCORM12`) to validate the upload/launch/completion pipeline against — US-1.1, A1 — Michelle Zuckerberg — `4839689`
- [x] [T5] Push initial mock frontend (static lesson data, home/lesson pages, lesson-card, app-shell) as the pre-Supabase scaffold — US-2.1 (scaffold only, later replaced) — Claire Cui (`cuic3`) — `9e41ed8`
- [x] [T6] Wire up Supabase MCP + client (`.mcp.json`, `src/utils/supabase.ts`) — infra — Rory Myers — `f89bed7`
- [x] [T7] Draft the technical plan — Supabase schema, RLS policies, SCORM runtime bridge design, same-origin content proxy design (`plan.md` §1–§6) — n/a (planning, traces to all of §2.1–§2.7) — Rory Myers — `5206c94`
- [x] [T8] Build the full P1 loop end-to-end: real login/role guards, admin upload + manifest validation + Storage upload, SCORM 1.2 API adapter + real iframe player + resume/bookmark, unified educator report, audit log on upload/deactivate — US-1.1, US-1.3, US-1.4, US-2.1, US-2.2, US-2.3, US-2.4, US-3.1, US-3.2, US-3.3, US-3.4, US-4.1, US-4.2 — Rory Myers — `4ded8bd`
- [x] [T9] Rebrand app to "Clinical Learning Hub" (header wordmark, login copy, typo fix) — n/a (polish) — Campbell Isherwood — `8ec7ca3`, `757739b`, `9b3bf46`
- [x] [T10] Enable real SCORM playback for Elsevier-origin lessons: generalize the player gate from `origin === 'custom'` to content-availability, author 4 real SCORM packages via the `generate-scorm-lessons` skill, backfill via `seed-elsevier-content.mjs` — US-2.1 (§3.3 scope update) — Rory Myers — `dd9d948`
- [x] [T11] Resolve spec-drift: align "no score reported" display text between `format-score.ts` and `spec.md` so code and spec say the same thing — US-3.3, §8 edge case — Michelle Zuckerberg — `0c2b238`
- [x] [T15] Build the full re-upload/replace flow: "Replace content" (new `lessons` row + old row `is_active=false`/`superseded_by_lesson_id`), "Reactivate", and metadata-only "Edit details", each logging its own `content_audit_log` action (`upload`+`previous_lesson_id`, `reactivate`, `edit`); lineage ("Replaces:"/"Replaced by:") shown in the admin lesson list — US-4.1 — Claude (pairing with Campbell Isherwood) — `275ed13`
- [x] [T17] Live score/status display: lesson-card now shows the current score inline (not just the status pill), and the lesson page polls the completion row every 2s while a SCO is running so "Your progress" reflects `LMSSetValue` writes without a manual "Refresh status" click — US-3.3 (display rule) — Claire Cui (`cuic3`) — `6d98e87`
- [x] [T18] Add source institution tracking for custom lessons: `lessons.source_institution` (admin-entered, free text) + `lesson_completions.source_institution_snapshot` (denormalized), captured on upload, shown in the admin lesson list and as a new "Source" column in the unified report (Elsevier rows show "—", blank custom rows show "Unknown"); new `cat-caretaking-basics` sample package; scope note + edge case + AC US-1.5 added to `spec.md` first, schema documented in `plan.md` §2.8 — US-1.5 (new) — Michelle Zuckerberg — `c494971`, reconciled with T15 in `0e30e27`
- [x] [T19] Reject non-SCORM-1.2 manifests at upload: `scorm-manifest.ts` now checks `<metadata><schemaversion>` and throws unless it's exactly `"1.2"` (was previously only checking for `imsmanifest.xml`'s presence, not its version) — spec §4 MUST ("only SCORM 1.2 is supported") — Claire Cui (`cuic3`) — `9c18d53`
- [x] [T20] Add a Vitest test suite covering utils, components, pages, and routing (16 test files: adapter, manifest parser, format-score, mime-types, auth-context, app-shell, lesson-card, require-role, and every page) — closes the "no automated tests" gap flagged in review — Rory Myers — `65384e9`
- [x] [T21] Extend the test suite for source-institution tracking and the reupload/reactivate flows (T15/T18) — Rory Myers — `1f3c49e`
- [x] [T22] Update manifest tests for the new SCORM 1.2 schema-version check (T19) — Rory Myers — `0141685`
- [x] [T28] Standardize lesson card sizing/layout: fixed card height, line-clamped title/description, score moved onto the same line as the launch/review link, content vertically centered so short cards don't look top-heavy — n/a (visual polish, no AC change) — Rory Myers — `9f63f04`
- [x] [T24] Gave Reactivate / Edit details / Replace content their own ACs (US-4.3/4.4/4.5), edge case, and Verify Table rows in `spec.md` §3.4/§8/§9, plus a `plan.md` §2.9 write-up — US-4.3, US-4.4, US-4.5 — Claude (this session)
- [x] [T25] Updated `plan.md` §6 build order to reflect what's actually built (content lifecycle + Elsevier playback marked DONE; fixed a duplicate "5." numbering bug) — n/a (doc fix) — Claude (this session)
- [x] [T26] Filled in `CLAUDE.md`'s "Stack & conventions" section from `plan.md` §1 — n/a (doc fix) — Claude (this session)
- [x] [T27] Amended `spec.md` §9 to explicitly carve out §9.1 as a deliberate append-only exception, instead of §9.1 silently violating §9's own rule — n/a (doc fix) — Claude (this session)
- [x] [T29] Fixed a spec-drift regression in `format-score.ts`: the "sign up created" commit had dropped "Completed —" from the no-score message repo-wide, re-breaking T11's fix. Split the wording by audience instead of picking one: `formatScoreCell` now takes an `{ audience: 'learner' | 'report' }` option — `report.tsx` keeps `"Completed — no score reported"` (spec.md §3.3), `lesson.tsx`/`lesson-card.tsx` now intentionally show the shorter `"No score reported"` (new AC US-2.5, per team decision). Also fixed the test suite, which had gone red/type-error on this same pull: added the missing `signUpLearner` mock to 7 test files' `useAuth` mocks (broken by the signup feature's `AuthState` change, never updated) and updated `lesson-card.test.tsx`'s stale assertion. Verified with `tsc --noEmit` + `npm test` (190/190) + `npm run build` — US-2.5, US-3.3 — Michelle Zuckerberg — this session
- [x] [T32] Built learner self-service signup: `/signup` route, `signup.tsx`, `auth-context.tsx`'s `signUpLearner` — built without a `spec.md` scope note or owner confirmation first, unlike every other feature in this list (see T30) — US-5.1 (added retroactively) — Claire Cui (`cuic3`) — `d96e0d5`
- [x] [T30] Documented the previously-unflagged T32 signup feature across `spec.md` (§3.5 new user story, §4 cross-reference, §7 new assumption A4, §8 edge case, §9 Verify row) and `plan.md` (§2.10), rather than fabricating a confirmation — A4 confirmed in-scope, see T36 (A4 itself later removed from §7 as a standalone entry — see T38) — US-5.1 — Michelle Zuckerberg — this session, `8b2e8f7`
- [x] [T33] Fixed signup's profile creation: `signUpLearner` now passes `display_name`/`organization_id` as Supabase Auth user metadata instead of inserting a `profiles` row directly from the client — implies a DB-side `auth.users` trigger now creates that row, but no such trigger is documented in `plan.md` §2.1 and this session has no live DB access to confirm one exists (flagged in `plan.md` §2.10 and `spec.md` §8 as unverified) — US-5.1 — Claire Cui (`cuic3`) — `054b3da`
- [x] [T34] Filled in `CLAUDE.md`'s "Stack & conventions" independently of T26 (both landed the same placeholder fix in parallel) — merged both versions rather than picking one, keeping T26's Node-version/test-runner note plus this commit's more detailed data-model-conventions section — n/a (doc fix) — Campbell Isherwood, merged by Michelle Zuckerberg — `fe1b5fd`, merge `145521d`
- [x] [T35] Redesigned the learner lesson card (`lesson-card.tsx`/`.scss`): grouped the "Custom content" badge and duration into the header, replaced the separate status pill/score text with inline status + score pills, and added a `PASSING_SCORE_PERCENT = 70` threshold that introduces a new "Failed" status (orange pill, "Retake lesson" CTA) for completed lessons scoring below 70% — shipped ahead of the spec, then backfilled into `spec.md` §3.2 (US-2.6, new), §8 edge case, and §9 Verify row as a confirmed scope addition (display-only derivation, no new stored status, report unaffected) — US-2.6 (new) — Rory Myers — `6fc627d`, spec backfill this session
- [x] [T12] Filled in `spec.md` §9/§9.1 Verify Table for real: every AC checked against live Supabase data (`list_tables`/`execute_sql`), the Vitest suite (16 files, 191/191 passing), `tsc -b --noEmit` (clean), and direct source review — no browser-automation tool was available this session, so rows that weren't re-run live say so explicitly in their Notes rather than claiming an untrue fresh click-through. All 22 ACs across §9/§9.1 verified PASS; surfaced that T23 was already resolved (see below), that a concurrent teammate fix (T14) has since made the not-started-row case robust in general rather than relying on existing seed data happening to already cover it, and that US-4.3/4.4/4.5 have never actually been exercised on the live DB (spun out as new task T37) — all US IDs — Claude (this session)
- [x] [T14] Fixed (closes the "Next" item below): `report.tsx` now fetches active `lessons` alongside `profiles`/`lesson_completions` and enumerates every learner × every active lesson, synthesizing a `"Not started"` row (from the live `lessons` row) for any pair with no completion — not just the specifically-seeded Elsevier rows T14 originally asked about. A completion whose lesson has since been deactivated/superseded still shows via a fallback pass (never drops history, US-4.1), and a learner with truly zero rows still gets the old "No lessons started yet" placeholder. 3 new tests added, all 11 pre-existing `report.test.tsx` tests still pass unchanged — US-3.1 — Claude (pairing with Campbell Isherwood) — `e56d848`
- [x] [T23] Discovered already resolved during T12's live-DB check: `list_tables` shows `lessons.source_institution` and `lesson_completions.source_institution_snapshot` both exist on the live schema, and real data confirms the write-through path works ("Cat Caretaking Basics" has `source_institution='Hackathon'`, its completion row has the matching snapshot). Marking done rather than leaving it flagged "Blocking" in this list when the evidence says otherwise — someone applied it after T18/the plan.md note was written, outside this session — US-1.5 — unassigned (applied by someone with live DB access, exact author/commit not tracked)
- [x] [T36] Resolved A4 (later removed as a standalone §7 entry, see T38 — this is the confirmation itself, which stands): learner self-service signup confirmed in scope by Claire Cui — `/signup` is not public-facing and only ever creates synthetic/seeded learner accounts against a seeded organization, so it does not conflict with §4's synthetic/seeded-accounts rule. Also verified end-to-end that the live Supabase project requires email confirmation and that the confirmation email is delivered and the signup → confirm → login path works. Updated `spec.md` (§3.5 scope note, §4 cross-reference, §7 A4 owner assigned, §8 edge case, §9 Verify row → PASS) and `plan.md` (§2.10, Alignment Check, §6 build order item 7, Verification section) accordingly — US-5.1 — Claire Cui — this session
- [x] [T38] Removed A4 as a standalone `spec.md` §7 entry: once T36 confirmed it, it became a settled scope decision, not an open/unresolved assumption — the exact same category as source-institution (Michelle) and Elsevier-real-playback (Rory), neither of which ever got its own §7 row; both live purely as an inline "Scope addition (confirmed with X)" note in their §3.x section. A4 was the one exception, duplicating a settled decision into the "open assumptions with a tripwire" table. Removed the §7 row; kept Claire's confirmation and reasoning exactly where the other two live (§3.5's scope note, §4's cross-reference); scrubbed the now-dangling "A4"/"see A4" labels from `spec.md` §8/§9 and `plan.md` §2.10/Alignment Check/§6/Verification (pointing at §3.5 instead) — flagged by Michelle Zuckerberg — Michelle Zuckerberg — this session
- [x] [T37] Exercised "Replace content" / "Reactivate" / "Edit details" against the live app at least once each — all three confirmed working — closing the gap T12 had flagged (Verify Table PASS previously rested on code review + unit tests only, not a live run) — US-4.3, US-4.4, US-4.5 — unassigned
- [x] [T40] Added "Export CSV" to the educator report (`report.tsx`): a client-side download of exactly the currently-displayed rows, built from the same `originText`/`sourceText`/`STATUS_LABEL`/`formatScoreCell` helpers the table itself uses (so CSV and screen can't drift), CSV-escaped for commas/quotes/newlines, no new backend endpoint. Spec updated first (`spec.md` §3.3 US-3.6 new AC + scope-addition note, §8 edge case for a zero-row report, §9 Verify row) per `CLAUDE.md`'s working agreement, confirmed with Rory Myers. 4 new tests in `report.test.tsx` (button disabled while loading, header-only export on an empty report, full-row export matching table formatting, comma-value quoting); full suite 198/198, `tsc -b --noEmit` clean, `npm run build` clean — US-3.6 (new) — Claude (this session), on `feature/educator-csv-export`
- [x] [T41] Closed the real-browser gap T40 flagged: added Playwright (`app/e2e/report-export-csv.spec.ts`, `playwright.config.ts`) as a new e2e layer alongside Vitest, confirmed with Rory Myers (Vitest/jsdom can't prove a real file download works — `URL.createObjectURL` is stubbed there). The test logs into the live Supabase project as the seeded `educator@demo.local` account (password supplied out-of-band into untracked `app/.env` as `E2E_EDUCATOR_EMAIL`/`E2E_EDUCATOR_PASSWORD`, never committed or typed into a tracked file), clicks Export CSV, and asserts a real downloaded file has the expected header + ≥1 data row. Passing locally (`npm run test:e2e`). `CLAUDE.md` Stack & conventions updated with the new E2E testing bullet; `vitest.config.ts` excludes `e2e/**` so the two runners don't collide — US-3.6 — Claude (this session), on `feature/educator-csv-export`
- [x] [T39] Added Skill 6 — Sync Docs With Commit to `skills.md`, documenting `/sync-docs-with-commit` (index entry + prompt block, matching the existing format) — n/a (doc fix) — Claude (this session)

## Reviews / Audits
<!-- QA passes against spec.md — don't build features, but every open item in "Next" traces to one of these. -->
- [x] [R1] Full spec-adherence review of the built app against every §3 AC, §8 edge case, and §6 out-of-scope item — surfaced: `metrics.md`/`tasks.md` still unfilled templates, `spec.md` §9/§9.1 Verify Table never filled in, `report.tsx` sourcing rows from `lesson_completions` instead of `lessons` (risk of dropping seeded "not started" rows from the report), re-upload/reactivate/edit unbuilt despite being speced in `plan.md`, and no automated test coverage — findings fed directly into T12, T13, T14, T15, T20 — Campbell Isherwood (session), reviewed by Claude — relayed in-session, not committed as a standalone artifact
- [x] [R2] External review agent re-audited 5 previously-flagged spec-drift items (dedicated AC/edge-case/Verify row for reactivate/edit-details/replace-content; stale "Polish" line in `plan.md` §6; `tasks.md` T15's "uncommitted" note; `CLAUDE.md`'s blank "Stack & conventions"; §9/§9.1's self-contradicting "don't invent new IDs" rule) against the current repo — found 1 of 5 actually fixed (T15's commit reference) — Campbell Isherwood (session)
- [x] [R3] Independently re-verified all 5 of R2's findings line-by-line against the current `spec.md`/`plan.md`/`CLAUDE.md` before acting on them (grep for "reactivate"/"edit details"/"replace content" in `spec.md`; read `plan.md:204`; read `CLAUDE.md:19`; re-read `spec.md` §9/§9.1) — confirmed all 4 open findings were real, not stale — turned into T24–T27 — Claude (this session)

### Thin Slice
Smallest subset that produced a working end-to-end path: T4, T6, T7, T8 (login → admin upload → real SCORM completion → educator report).

## Next
<!-- Gaps identified by re-checking the running build against spec.md — not yet started. -->
- [ ] [T13] Log an actual `metrics.md` checkpoint (tasks planned/completed, ACs passing, deviations, AI-vs-human notes) — spec §4 MUST — unassigned

## Cut / Deferred
<!-- Keep the reason. Scope change should remain visible. -->
- ~~[T16] Multiple learners/packages/assignments beyond the single P1 loop~~ — deferred per assumption A3 (one learner/one package sufficient to prove the loop, owner Rory Myers)
